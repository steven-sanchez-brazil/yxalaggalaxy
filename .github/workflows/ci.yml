name: "Validación de Frontend"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout del código"
        uses: actions/checkout@v4

      - name: "Validación básica"
        run: echo "Proyecto cargado correctamente. Archivos index.html, script.js, styles.css"
